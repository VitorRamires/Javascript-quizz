import styled from "styled-components";

export const StatisticStyled = styled.div`
  .statistic-header {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 45px;
    margin-top: 25px;
  }

  .log {
    margin-top: 50px;
  }
`;
